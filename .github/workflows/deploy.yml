name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    uses: GY-CODING/.github/.github/workflows/next-deploy.yml@master
    secrets: inherit
    with:
      domain: 'heraldsofchaos.gycoding.com'
